import { Identity } from '../identity';
export declare function validateIdentity(identity: Identity): string;
declare const _default: {
    validateIdentity: typeof validateIdentity;
};
export default _default;
